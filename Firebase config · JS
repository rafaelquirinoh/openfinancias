/* ==========================================================================
   OPEN FINANCING — firebase-config.js
   ==========================================================================
   Este arquivo conecta o OPEN FINANCING à sua conta do Firebase para permitir
   login com Google e salvar os dados na nuvem (um espaço separado por usuário).

   COMO PREENCHER (gratuito, leva uns 5 minutos):
   1. Acesse https://console.firebase.google.com e crie um projeto novo.
   2. No menu lateral, vá em "Compilação" > "Authentication" > "Sign-in method"
      e ative o provedor "Google".
   3. No menu lateral, vá em "Compilação" > "Firestore Database" > "Criar banco
      de dados" (pode escolher o modo produção).
   4. Ainda no Firestore, na aba "Regras", cole o conteúdo do arquivo
      firestore.rules que também foi entregue com este projeto, e publique.
   5. No menu lateral, clique na engrenagem > "Configurações do projeto".
      Na aba "Geral", role até "Seus apps", clique no ícone "</>" (Web),
      dê um nome ao app e registre. O Firebase vai te mostrar um objeto
      "firebaseConfig" parecido com o de baixo — copie os valores para cá.
   6. Salve este arquivo. Pronto: o botão "Entrar com Google" no app passa
      a funcionar de verdade.

   Sem preencher estes valores, o app continua funcionando 100% normalmente
   no modo local (LocalStorage) — só o login com Google que fica inativo.
   ========================================================================== */

const firebaseConfig = {
  apiKey: "COLE_AQUI_SUA_API_KEY",
  authDomain: "COLE_AQUI_SEU_PROJETO.firebaseapp.com",
  projectId: "COLE_AQUI_SEU_PROJECT_ID",
  storageBucket: "COLE_AQUI_SEU_PROJETO.appspot.com",
  messagingSenderId: "COLE_AQUI_SEU_SENDER_ID",
  appId: "COLE_AQUI_SEU_APP_ID"
};